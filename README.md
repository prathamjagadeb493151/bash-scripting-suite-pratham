# system-monitor-tool-pratham
A C++ based System Monitor Tool (Capstone Project) that displays real-time CPU usage, memory utilization, uptime, and running processes using the /proc filesystem and ncurses library on Linux (Ubuntu/WSL).
